##### app.py是一个flask框架，其余的是基于streamlit的前端交互界面

## Run it locally

```sh
virtualenv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run Hello！🤠.py
```
